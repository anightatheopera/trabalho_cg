#include <vector>

#include "points.h"

std::vector<Point> draw_cone(double radius, double height, int slices, int stacks){
    std::vector<Point> points;
    //undefined behavior
    return points;
}